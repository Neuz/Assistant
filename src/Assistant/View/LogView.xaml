<UserControl x:Class="Assistant.View.LogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Label Grid.Row="0" Margin="10,5,10,0" FontSize="18" FontWeight="Bold" Content="日志" />
        <Button Grid.Row="0" Content="清空" Width="40" Height="30" HorizontalAlignment="Right" Margin="10,5,10,0"
                Click="Clear_Click" />
        <Separator Margin="10,0,10,0" Grid.Row="1" Height="2" HorizontalAlignment="Stretch" />
        <RichTextBox
            Grid.Row="2"
            x:Name="RichTextBox"
            FontSize="14"
            IsReadOnly="True"
            VerticalScrollBarVisibility="Auto"
            HorizontalScrollBarVisibility="Auto"
            Margin="10,10,10,5"
        />
    </Grid>
</UserControl>
